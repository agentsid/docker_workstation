version: "3"
services:
  springboot-app:
    #container_name: 
    image: springboot:latest
    #build: .
    ports:
      - 8000-8999:8080

  mysqldb:
    container_name: mysqldb
    image: mysql:latest
    ports:
      - 3307: 3307
    environment:
      MYSQL_DATABASE: test
      MYSQL_ROOT_PASSWORD: root
    